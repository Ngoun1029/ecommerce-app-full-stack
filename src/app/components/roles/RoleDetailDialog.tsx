import { DetailProps } from "@/app/types/DetailProps";
import { useEffect } from "react";

export default function RoleDetailDialog({ open, setOpen, id }: DetailProps) {
    useEffect(() => {
      
    })
  return <div>role detail</div>;
}
